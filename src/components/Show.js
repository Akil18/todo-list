import React, { useContext } from 'react';
import { StoreContext } from '..';

const Show = () => {
    const {store} = useContext(StoreContext);

    return (
        <div>
            
        </div>
    );
};

export default Show;